item do
  expected do
    url_xpath
    url 'http://git.savannah.gnu.org/cgit/libidn.git/refs/tags'
    xpath '//*[@id="cgit"]/div/table/tr[2]/td[1]/a'
    regex /^libidn-(\d+).(\d+)$/, '\1.\2'
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
